
export const metadata = {
    title: "Manage Room Change Requests",
    description: "Manage Room Change Requests Page",
};

export default function CreateUserLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>{children}</>
    );
}


export const metadata = {
    title: "Mange Room Request",
    description: "Mange Room Request Page",
};

export default function CreateUserLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>{children}</>
    );
}
